{
  "name": "antdsite-default-theme",
  "version": "0.0.0",
  "description": "The default theme of Antdsite",
  "private": true,
  "scripts": {
    "build": "yarn clean && gatsby build",
    "start": "yarn clean && gatsby develop",
    "clean": "gatsby clean"
  },
  "devDependencies": {
    "antdsite": "latest",
    "gatsby": "^2.13.45"
  }
}
